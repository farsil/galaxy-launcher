<Window x:Class="DosboxLauncher.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:DosboxLauncher.ViewModel"
        d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <ListView ItemsSource="{Binding Path=Programs}">
        <ListView.View>
            <GridView>
                <GridViewColumn Width="300" DisplayMemberBinding="{Binding Path=Path}" Header="Path" />
                <GridViewColumn Width="300" DisplayMemberBinding="{Binding Path=Title}" Header="Title" />
            </GridView>
        </ListView.View>
    </ListView>
</Window>
